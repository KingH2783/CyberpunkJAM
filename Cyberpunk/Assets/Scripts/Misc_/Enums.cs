using UnityEngine;

namespace HL
{
    public enum ActionMaps
    {
        None,
        Menu,
        Player
    }

    public enum AIType
    {
        BasicMelee,
        BasicRanged,
        Heavy,
        FastGrounded,
        FastFlying,
        Boss
    }
}